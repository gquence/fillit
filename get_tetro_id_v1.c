#include "fillit.h"
#include <stdio.h>

unsigned short int check_ztetro(const int tetro)
{
	int z_vert;
	int z_horiz;

	z_vert = 19584;
	z_horiz = 50688;
	if (!(tetro - z_vert))
		return (z_vertical);
	if (!(tetro - z_horiz))
		return (z_horizontal);
	return (0);	
}

unsigned short int check_stetro(const int tetro)
{
	int s_vert;
	int s_horiz;

	s_vert = 35904;
	s_horiz = 27648;
	if (!(tetro - s_vert))
		return (s_vertical);
	if (!(tetro - s_horiz))
		return (s_horizontal);
	return (0);
}

unsigned short int check_ltetro(const int tetro)
{
	int t_u;
	int t_d;
	int t_l;
	int t_r;

	t_u = ;
	t_d = ;
	t_l = ;
	t_r = ;
	if (!(tetro - t_u))
		return ();
	if (!(tetro - t_u))
		return ();
	if (!(tetro - t_u))
		return ();
	if (!(tetro - t_u))
		return ();
	return (0);
}

unsigned short int check_jtetro(const int tetro)
{
	int t_u;
	int t_d;
	int t_l;
	int t_r;

	t_u = ;
	t_d = ;
	t_l = ;
	t_r = ;
	if (!(tetro - t_u))
		return ();
	if (!(tetro - t_u))
		return ();
	if (!(tetro - t_u))
		return ();
	if (!(tetro - t_u))
		return ();
	return (0);
}

unsigned short int check_ttetro(const int tetro)
{
	int t_u;
	int t_d;
	int t_l;
	int t_r;

	t_u = ;
	t_d = ;
	t_l = ;
	t_r = ;
	if (!(tetro - t_u))
		return ();
	if (!(tetro - t_u))
		return ();
	if (!(tetro - t_u))
		return ();
	if (!(tetro - t_u))
		return ();
	return (0);
}

int	main()
{
	
}
